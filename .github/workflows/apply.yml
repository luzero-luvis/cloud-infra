name: Apply

on:
  push:
    branches: [main]
    paths:
      - "envs/**"
      - "modules/**"

permissions:
  id-token: write
  contents: read

env:
  TF_VERSION: "1.10.5"

jobs:

  # ── JOB 1 ───────────────────────────────────────────────────────────────────
  # Apply dev first — always deploy to dev before prod
  # ────────────────────────────────────────────────────────────────────────────
  apply-dev:
    name: apply dev
    runs-on: ubuntu-latest
    environment: dev
    defaults:
      run:
        working-directory: envs/dev
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_DEV }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

<<<<<<< feature/add-ci-pipeline
=======
      # Connect to S3 backend and download the current state file
>>>>>>> main
      - name: terraform init
        run: terraform init

      - name: terraform apply
        run: terraform apply -auto-approve -no-color


  # ── JOB 2 ───────────────────────────────────────────────────────────────────
  # Plan prod AFTER dev succeeds — saves the plan so the approver can review
  # exactly what will change before clicking Approve
  # ────────────────────────────────────────────────────────────────────────────
  plan-prod:
    name: plan prod
    runs-on: ubuntu-latest
    needs: apply-dev
    defaults:
      run:
        working-directory: envs/prod
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_PROD }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: terraform init
        run: terraform init

      - name: terraform plan
        run: terraform plan -out=tfplan.binary -no-color 2>&1 | tee plan.txt

      # Upload both the binary plan (used by apply) and the text plan (for the approver to read)
      - name: Upload plan artifact
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-prod-${{ github.sha }}
          path: |
            envs/prod/tfplan.binary
            envs/prod/plan.txt
          retention-days: 7


  # ── JOB 3 ───────────────────────────────────────────────────────────────────
  # Apply prod — PAUSES here until a human clicks Approve in GitHub UI
  # Approver should download the plan artifact from Job 2 to review before approving
  # Applies the EXACT saved plan file — not a fresh plan — so no drift between
  # what was reviewed and what actually runs
  # ────────────────────────────────────────────────────────────────────────────
  apply-prod:
    name: apply prod
    runs-on: ubuntu-latest
    needs: plan-prod
    environment: production
    defaults:
      run:
        working-directory: envs/prod
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_PROD }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: terraform init
        run: terraform init

      # Download the saved plan from Job 2
      - name: Download plan artifact
        uses: actions/download-artifact@v4
        with:
          name: tfplan-prod-${{ github.sha }}
          path: envs/prod

      # Apply the saved plan — not a fresh terraform apply
      # This guarantees exactly what the approver reviewed is what runs
      - name: terraform apply
<<<<<<< feature/add-ci-pipeline
        run: terraform apply -no-color tfplan.binary
=======
        run: terraform apply -auto-approve -no-color
>>>>>>> main
